using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Helloworldt : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Вывод привет мир
        print("Hello world!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
